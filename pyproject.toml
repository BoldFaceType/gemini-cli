[tool]
name = "gemini-cli"
version = "0.1.0"
description = "A command-line interface for the Gemini project."
authors = ["Your Name <you@example.com>"]
license = "MIT"

[tool.poetry]
dependencies = [
    "click",
    "requests",
    "toml",
]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"